create database ss4_kha2;
create  table products(
       id serial primary key ,
       name varchar(50),
       category varchar(50),
       price decimal(10,2),
       stock int
);

insert into products
values (default,'Laptop Dell','Electronics',1500.00,5),
       (default,'Chuột Logitech','Electronics',25.50,50),
       (default,'Bàn phím Razer','Electronics',120.00,20),
       (default,'Tủ lạnh LG','Home Appliances',800.00,3),
       (default,'Máy giặt Samsung','Home Appliances',600.00,2);

select * from products;

insert into products values (default,'Điều hòa Panasonic','Home Appliances',400,10);

update products set stock = 7 where name = 'Laptop Dell';

delete from products where stock =0;

select products.name from products order by price ASC ;

select distinct products.category from products;

select products.name from products where price between 100 and 1000;

select products.name from products where name like '%LG%' or name like '%Samsung%';

select products.name, products.price from products order by price desc limit 2;

select products.name, products.price from products limit 2 offset 1;


